call pswap#modulelib#makeModule(s:, 'projects', 'base')

" function! s:f.saveSession(action, ...) dict abort
"     echo "saving session"
" endfunction

" call s:f.addCommand('saveSession', ['save'], 'PswapSaveSession', ['s'], 'Saves all buffers in current folder to a session (will overwrite current session).')
